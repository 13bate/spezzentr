import clsx from "clsx";
import style from "./Competition.module.scss"

interface Props {
  className?: string;
}

export const Competitions: React.FC<Props> = ({ className }) => {
  return (
    <div className={clsx()}>

    </div>
  )
}
